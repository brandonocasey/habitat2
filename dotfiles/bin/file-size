#!/usr/bin/env bash

file_size() {
  wc -c "$1" | sed 's~ ~~g'
}

# if this is not an interactive shell read from pipe
if ! tty >/dev/null; then
  while read -r line; do
    file_size "$line"
  done;
fi

while [ "$#" -gt 0 ]; do
  file_size "$1"; shift
done

